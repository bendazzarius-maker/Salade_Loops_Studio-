/* ================= Electro DAW | app.js (Bootstrap) ================= */
/* This file is intentionally minimal because the project uses classic scripts (no bundler).
   Boot logic is executed in boot.js after all modules are loaded. */
